fname = input("Enter file name: ")
fh = open(fname)
lst = list()
for line in fh:
    word = line.split()
    for x in word:
        if x in lst : continue
        lst.append(x)
        
lst.sort()
print(lst)

    

  
